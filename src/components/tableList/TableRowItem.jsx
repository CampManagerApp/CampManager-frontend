export default function TableRowItem({items, onClick, onUpdate, onDelete}) {
    return (
        <tr onClick={() => {
            setitem(org)
            setModalShow(true)
        }}>
        </tr>
    )
}